P :  Understanding of vim editor

 # SP => Implement a stop watch in vim editor status line
  # Print time in vim editor status line
   # .vimrc => set ruler, 
       set rulerformat=%55(%{strftime('%a\ %b\ %e\ %I:%M\ %p')}\ %5l,%-6(%c%V%)\ %P%) 
   # Change the ruler format to hold just minutes
    # Update the ruler to print 000
    # Update this value to hold a variable 
    
          
